import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App se inicializa', (WidgetTester tester) async {
    // Placeholder - los tests de integracion se agregan mas adelante.
    expect(true, isTrue);
  });
}
